export enum SocketEvent {
    ROOM_JOIN = 'ROOM_JOIN',
    ROOM_LEFT = 'ROOM_LEFT',
    NOW = 'NOW',
    KLINE = 'KLINE',
    disconnect = 'disconnect',
    USER = 'USER',
    connect = 'connect',
    NOTIFICATION = 'NOTIFICATION',
    TRADING_CONFIG = 'TRADING_CONFIG',
}
